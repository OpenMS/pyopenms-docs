name: Linting code-blocks
on: [pull_request]
jobs:
  linting:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - uses: actions/setup-python@v4
      with:
        python-version: '3.10' # Version range or exact version of a Python version to use, using SemVer's version range syntax

    - name: Installing Dependencies
      run: |
        pip install blacken-docs


    - name: Get changed files
      id: changed-files
      uses: tj-actions/changed-files@v35

#    - name: Running blacken
#      run: blacken-docs -l 80 ${{ steps.changed-files.outputs.all_changed_files }}

    - name: run action-suggester
      uses: reviewdog/action-suggester@v1
      with:
        tool_name: blacken-docs -l 80 ${{ steps.changed-files.outputs.all_changed_files }}